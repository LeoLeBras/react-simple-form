/* @flow */

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    marginVertical: 3,
    padding: 10,
    backgroundColor: '#eee',
  },
  text: {
    textAlign: 'center',
  },
})

export default styles
